
const LOG_IN = 'LOG_IN';
const LOG_OUT = 'LOG_OUT';

const AUTH_INFO = 'AUTH_INFO';

const LOADING = 'LOADING';

const types = {
    LOG_IN,
    LOG_OUT,
    AUTH_INFO,
    LOADING
}

export default types;
